# Infrastructure

This folder holds deployment and container scaffolding for APIfyn.

Use `docker/docker-compose.dev.yml` for local Postgres/API composition and `docker/docker-compose.prod.yml` as the production starting point.
